{
  "name": "metalsmith-fs-metadata",
  "description": "A metalsmith plugin to load global metadata from files relative to the root folder.",
  "repository": "git://github.com/dw-buildit/metalsmith-fs-metadata.git",
  "version": "0.0.4",
  "license": "MIT",
  "main": "lib/index.js",
  "dependencies": {
    "js-yaml": "^3.13.1"
  },
  "devDependencies": {
    "metalsmith": "^2.3.0",
    "mocha": "^6.2.2"
  },
  "scripts": {
    "test": "mocha"
  }
}
